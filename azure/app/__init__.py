"""CTS Scoreboard Azure relay app."""

__version__ = "0.1.0"

# Protocol version exchanged with Pi and browsers at handshake.
# Bump CURRENT when the wire protocol changes; update MIN_SUPPORTED only
# when removing backwards compatibility.
PROTOCOL_VERSION_CURRENT = 1
PROTOCOL_VERSION_MIN_SUPPORTED = 1
